<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lihaiyang.learn.dao.RoleDao">


    <sql id="roleColumns">
    	a.id,
    	a.name,
    	a.enname,
		a.remarks,
		a.del_flag,
    </sql>

    <select id="findByUserId" resultType="com.lihaiyang.learn.entity.Role">
        SELECT DISTINCT
        <include refid="roleColumns"/>,
        FROM sys_role a
        LEFT JOIN sys_user_role ur ON ur.role_id = a.id
        LEFT JOIN sys_user u ON u.id = ur.user_id AND u.id = #{userId}
        WHERE a.del_flag = #{DEL_FLAG_NORMAL} AND u.del_flag = #{DEL_FLAG_NORMAL}
    </select>

</mapper>
